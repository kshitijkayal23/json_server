{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Mock API using JSON Server",
  "main": "server.js",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server --watch data.json --port 8080"
  }
}
